package com.RK.mediaSequencer.repository;

import com.RK.mediaSequencer.model.DisplayWindow;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DisplayWindowRepo extends JpaRepository<DisplayWindow, Long> {
}
